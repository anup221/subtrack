spring:
  application:
    name: subtrack
  datasource:
    url: jdbc:postgresql://localhost:5432/subtrack
    username: subtrack
    password: subtrack
  jpa:
    hibernate:
      ddl-auto: validate   # Flyway owns the schema, not Hibernate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: "replace-this-with-a-long-random-string-at-least-32-chars-for-hs256"
  expiration-ms: 86400000   # 24 hours

webhook:
  secret: "dev-webhook-shared-secret-change-in-prod"

razorpay:
  key-id: "rzp_test_TVzjLsAJRjEm3d"
  key-secret: "uqJLWCICpj98PPV7IjdsdeUI"
  webhook-secret: "not-set-up-yet"

server:
  port: 8080